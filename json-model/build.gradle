apply plugin: 'java-library'
apply from: "${rootDir}/gradle/publication.gradle"

ext { pomDescription = 'Java model for Gradle plugin' }

dependencies {
	implementation "com.google.guava:guava:${guavaVersion}"
}
