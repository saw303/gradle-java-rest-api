dependencies {

	api project(':rest-model')
	implementation "org.slf4j:slf4j-api:${slf4jVersion}"

	compileOnly "org.springframework:spring-webmvc:${springVersion}"
}

ext { pomDescription = 'Add ons for the Spring platform' }
